extends layout
block content
    div#popback.popback
        div#popen.popen This is a popen
    div.container
        div.board.board-large(board="configs")
        div.board.board-small(board="mailsummary")
        div.board.board-small(board="mailsummary")
        div.board.board-small(board="mailsummary")
        div.board.board-small(board="mailsummary")
        div.board.board-medium(board="configs", target-url="#{basedomain}/board/jobs")
block script
    script(src='/js/loadboard.js')