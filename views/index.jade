extends layout
block content
    div#popback.popback
        div#popen.popen This is a popen
    div.container
        div.board.board-large(board="configs")
        div.board.board-small(board="mailsum")
        div.board.board-small(board="mailsum")
        div.board.board-small(board="mailsum")
        div.board.board-small.bp(board="mailsum", target-url="#{basedomain}/board/jobs")
        div.board.board-medium.bp(board="jobsum", target-url="#{basedomain}/board/jobs")
        div.board.board-medium.bp(board="configs", target-url="#{basedomain}/board/jobs")
block script
    script(src='/js/util.js')
    script(src='/js/loadboard.js')